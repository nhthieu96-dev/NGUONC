{
  "name": "nguonc-stremio-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js",
    "test": "node --test",
    "build": "wrangler deploy --dry-run --outdir dist",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": { "wrangler": "^4.0.0" }
}
